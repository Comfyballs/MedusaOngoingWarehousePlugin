/dist
.env
# Live integration-test secrets (Ongoing sandbox creds). The *.example is committed.
.env.integration
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa

# graphify knowledge graph output
/graphify-out

# local-only Claude Code settings (e.g. graphify hook)
.claude/settings.local.json

# transient git worktrees created for isolated agent runs
.claude/worktrees/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Local superpowers uninstall backups
.superpowers-uninstall-backup-*.tar.gz
